{A97 - listGenerator}

bubbleSort(x:List):List
[* Sortiert die Liste <x> aufsteigend mit dem Sortierverfahren Bubble-Sort. *]
::= x.bubbleSortIterator(x.size);

bubbleSortIterator(x:List, ---n:Card):List
[* Gibt die Liste nach <n> Sortierdurchläufen zurück. *]
::= x;
>>>bubbleSortIterator(x:NotEmptyList, n:Card):List
::= n.=(1).?(
		then --> x,
		else --> [iterations --> n.-C-]
							x.bubbleSortIteration(iterations).bubbleSortIterator(iterations));

bubbleSortIteration(x:List, ---n:Card):List
[* Ein Bubble-Sort Durchlauf für die ersten <n> Elemente. *]
::= x;
>>>bubbleSortIteration(x:MultiElementList, n:Card):List
::= n.=(0).?(
		then --> x,
		else --> x.first.lessEq(x.rest.first).?(
				then --> x.first.notEmptyList(x.rest.bubbleSortIteration(n.-C-)),
				else --> x.rest.first.notEmptyList(x.first.notEmptyList(x.rest.S_).bubbleSortIteration(n.-C-))));